{
  "name": "generator-test",
  "author": "George L Sun",
  "version": "1.0.0",
  "description": "test",
  "files": [
    "generators"
  ],
  "scripts": {
    "test": "mocha -u bdd -R spec -t 500 --recursive",
    "watch": "mocha -u bdd -R spec -t 500 --recursive --watch"
  },
  "keywords": [
    "yeoman-generator"
  ],
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "fs-extra": "^9.0.0",
    "mkdirp": "^1.0.3",
    "mocha": "^7.1.1",
    "yeoman-assert": "^3.1.1",
    "yeoman-test": "^2.3.0"
  },
  "dependencies": {
    "common-tags": "^1.8.0",
    "rimraf": "^3.0.2"
  }
}
